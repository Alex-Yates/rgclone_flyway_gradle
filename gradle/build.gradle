task printHelloWorld(type: Exec) {
    def command = System.getProperty('os.name').toLowerCase().contains('win') ? 'echo Hello, World!' : 'echo "Hello, World!"'
    commandLine 'cmd', '/c', command
}

// Run the printHelloWorld task
defaultTasks 'printHelloWorld'