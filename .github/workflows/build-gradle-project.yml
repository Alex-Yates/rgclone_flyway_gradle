name: Build Gradle project

on:
  push:

jobs:
  build-gradle-project:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout project sources
      uses: actions/checkout@v2
    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
    - name: Setup rgClone
      run: |
        chmod +x ./gradle/gradlew
        cd ./gradle
        ./gradlew setupRgCloneCli -PrgCloneEndpoint="${{ secrets.RGCLONE_ENDPOINT }}" -PrgCloneToken="${{ secrets.RGCLONE_TOKEN }}"
    - name: Create Data-Container (lifetime 5m)
      run: |
        BUILD_ID=${{ github.run_id }}
        echo "Container Name: test_${BUILD_ID}"
        cd ./gradle
        ./gradlew createDataContainer -PrgCloneEndpoint="${{ secrets.RGCLONE_ENDPOINT }}" -PcontainerName="test_build_${BUILD_ID}"
    - name: View all rgClone images and containers
      run: |
        cd ./gradle
        ./gradlew getAll -PrgCloneEndpoint="${{ secrets.RGCLONE_ENDPOINT }}"